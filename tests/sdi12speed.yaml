#Example configuration file for DataBear
#Create a new file with specific settings
#Must include datalogger and sensors keys 

#Defines data logger settings
#Settings key must have at least one set of storage settings
datalogger:
  name: test3sensors
  settings:
    - store: speed  #Measurement to be stored
      sensor: WSD      #Sensor that measurement is associated with
      process: None           #Not yet implemented. Leave as None
      frequency: 2           #Data STORAGE frequency in seconds
      #  - store: 'y'
      #    sensor: SIM
      #    process: None
      #    frequency: 10
      #  - store: airT
      #    sensor: TPH
      #    process: None
      #    frequency: 20

#Must have at least one set of measurement settings per sensor.
#Key value pairs in settings vary by method but must include
#serial number and measurement frequency.
sensors: 
#  - name: TPH
#    sensortype: dyaconTPH1
#    settings:     #Serial number and measurement are mandatory
#      serialnumber: '6166'
#      measurement: 10   #MEASUREMENT frequency in seconds
#      port: SM2       #Next settings are specific to measurement method
#      address: 4
  - name: 'WSD'
    sensortype: dyaconWSD2
    settings:     #Serial number and measurement are mandatory
      serialnumber: '1111'
      measurement: 0.5   #MEASUREMENT frequency in seconds
      port: SM4       #Next settings are specific to measurement method
      address: '0'
      #  - name: SIM
      #    sensortype: simStream
      #    settings:     #Serial number and measurement are mandatory
      #      serialnumber: '5555'
      #      measurement: 5   #MEASUREMENT frequency in seconds
      #      port: SM6       #Next settings are specific to measurement method
      #      baud: 19200
    

